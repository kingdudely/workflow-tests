name: Test Live Notice

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test notice
        run: |
          sleep 5

          echo "::notice title=🖥️ Remote Desktop Ready::https://example.trycloudflare.com"

          echo "Notice sent. Step will keep running for 2 minutes..."

          sleep 115
